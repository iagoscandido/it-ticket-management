# Personal It Tickets Management

## Requirements

- Node.js 24.14.0 LTS
- Docker Desktop

## How to run

1. ```sh
   docker compose up
   ```

2. ```sh
   npm i
   ```

3. ```sh
   # starts a local server on http://localhost:8080
    npm run dev
   ```
